export type ImageConfigType = {
  size: number;
  type: string[];
  // path: string;
};

export const AvatarConfig = {
  size: 1024 * 1024 * 2,
  type: ["image/jpeg", "image/png"],
  // path: "users/avatar",
};
